# there's an r package for everything
# when searching for a package put R package at end - "data storage mechanism R package"
# interact with... file, url, gzfile, bzfile
# foreign package loads data from minitab, S, SAS, SPSS, Stata, Systat
# db packages... RPostresSQL, RODBC, RMongo, rmongodb
# read images... jpeg, readbitmap, png, EBImage
  # load images into r and manipulate 
# read GIS data... rdgal, rgeos, raster
# read music files... tuneR, seewave